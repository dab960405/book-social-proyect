name: Deploy Backend to Koyeb

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl jq unzip

      - name: Install Koyeb CLI (Stable Binary)
        run: |
          set -e
          echo "📦 Descargando e instalando Koyeb CLI..."
          curl -sL https://get.koyeb.com | bash
          echo "✅ Instalación completada"
          echo "$HOME/.koyeb/bin" >> $GITHUB_PATH
          koyeb version || echo "Verificando instalación..."

      - name: Deploy to Koyeb
        env:
          KOYEB_API_TOKEN: ${{ secrets.KOYEB_API_TOKEN }}
        run: |
          echo "🚀 Desplegando aplicación en Koyeb..."
          koyeb app deploy my-app \
            --git github.com/${{ github.repository }} \
            --branch main \
            --token $KOYEB_API_TOKEN

